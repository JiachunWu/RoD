# Task Plan

## Goal

将 `/data/wjc/ultralytics-yolo11-main` 打包为一个适合上传 GitHub 的最简版本，所有打包结果放入新目录中，不修改原项目。只保留复现实验所需的最小代码、配置和说明文件，不上传数据集、训练权重、运行产物和大体积无关内容；同时补充数据集简介与获取方式说明。

## Tasks

- [x] T1: 在新目录初始化 `task.md` 和 `context.md`，记录目标、约束和执行计划。
- [x] T2: 读取原项目 `context.md` 与关键源码，确认最简公开版本必须保留的代码、配置和说明文件范围。
- [x] T3: 将最小可公开代码、配置和必要脚本复制到新目录，避免修改原项目文件。
- [x] T4: 在新目录补充 `README.md`、数据集说明和 `.gitignore`，明确不上传数据集、权重和实验输出。
- [x] T5: 验证新目录结构与关键文件完整性，更新 `task.md` 与 `context.md`，输出最终打包结果。

## Notes

- 必须先仔细阅读原项目 `/data/wjc/ultralytics-yolo11-main/context.md` 再确定打包范围。
- 不直接修改原项目中的任何文件。
- 新建目录必须包含新的 `task.md` 和 `context.md`。
- 只打包代码和必要文档，不上传数据集、模型权重、训练日志、推理结果、缓存和临时文件。
- 可在说明文档中简单介绍使用过的 `Dental_X` 与 `PDR-10` 数据集，以及其获取方式或组织方式。
